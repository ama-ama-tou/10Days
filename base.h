#pragma once
#include"Vec2.h"

const Vec2 kWindowSize(1280.0f, 720.0f);

const Vec2 kFieldLtPos(320.0f, 40.0f);
const Vec2 kFieldSize(640.0f, 640.0f);

const int kBeforeColor = 0x53558bff;
const int kAfterColor= 0xe28f8fff;